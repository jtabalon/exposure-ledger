{
  "name": "exposure-ledger",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "pnpm --filter @exposure-ledger/web build",
    "dev": "concurrently --kill-others-on-fail --names api,worker,web --prefix-colors cyan,magenta,green \"EXPOSURE_LEDGER_ENABLE_LOCAL_DISPOSITIONS=true PYTHONPATH=apps/api/src:packages/storage/src uv run uvicorn exposure_ledger_api.main:app --host 127.0.0.1 --port 8000 --no-proxy-headers --reload\" \"PYTHONPATH=apps/worker/src:packages/storage/src uv run python -m exposure_ledger_worker.main\" \"EXPOSURE_LEDGER_ENABLE_LOCAL_DISPOSITIONS=true pnpm --filter @exposure-ledger/web dev\"",
    "lint": "pnpm --filter @exposure-ledger/web lint",
    "test": "pnpm --filter @exposure-ledger/web test",
    "typecheck": "pnpm --filter @exposure-ledger/web typecheck"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
