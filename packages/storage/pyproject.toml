[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "exposure-ledger-storage"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "pydantic-settings>=2.8",
  "psycopg[binary]>=3.2",
]

[tool.hatch.build.targets.wheel]
packages = ["src/exposure_ledger_storage"]
